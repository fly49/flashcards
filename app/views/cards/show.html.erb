<%= render partial: 'content', locals: {card: @card} %>
<div class="modal-footer">
  <%= button_to 'Edit', edit_card_path(@card), method: :get, class: 'btn btn-primary'  %>
  <%= button_to "Delete", card_path(@card), method: :delete,
                               class: 'btn btn-primary' %>
</div>
